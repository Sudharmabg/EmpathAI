# Analogy Storyteller - Admin Implementation Plan

This plan details the steps to implement the **Analogy Storyteller** tool in the admin panel. It covers Java enum updates, Python AI schemas and prompts, and the React Admin UI editor and previewer.

## Proposed Changes

### 1. Spring Boot Backend
#### [MODIFY] [AiTaskType.java](file:///c:/empathai_updated_new/EmpathAI/EmpathaiBackend/src/main/java/com/empathai/curriculum/entity/AiTaskType.java)
- Add `ANALOGY` to the enum:
  ```java
  public enum AiTaskType {
      FLASHCARDS, SUMMARY, MNEMONIC, MOCK_TEST, ANALOGY
  }
  ```
  *(Note: Since taskType is annotated with `@Enumerated(EnumType.STRING)` in the database entity, no SQL schema migrations are needed).*

---

### 2. Python AI Service
#### [MODIFY] [validators.py](file:///c:/empathai_updated_new/EmpathAI/EmpathaiAI/curriculum/validators.py)
- Create new validation schemas:
  - `AnalogyDetails`: `title: str`, `base: str`, `description: str`, `imageUrl: Optional[str] = None`
  - `StoryDetails`: `title: str`, `narrative: str`, `moral: str`
  - `AnalogyStoryItem`: `concept: str`, `analogy: AnalogyDetails`, `story: StoryDetails`
  - `AnalogyOutput`: `stories: list[AnalogyStoryItem]`
- Add `"ANALOGY": AnalogyOutput` to `TASK_VALIDATORS` map.

#### [NEW] [analogy.txt](file:///c:/empathai_updated_new/EmpathAI/EmpathaiAI/prompts/analogy.txt)
- Create a prompt template instructing GPT-4o-mini to generate 2 to 3 analogies and stories for the selected topic/chapter, returning the structured JSON.

#### [MODIFY] [rag_orchestrator.py](file:///c:/empathai_updated_new/EmpathAI/EmpathaiAI/curriculum/rag_orchestrator.py)
- Register `ANALOGY` in configuration maps:
  - `TASK_MODELS`: `"gpt-4o-mini"`
  - `TASK_TEMPERATURES`: `0.5`
  - `TASK_MAX_TOKENS`: `3000`
- Register `ANALOGY` search query in `_build_query`:
  `"ANALOGY": f"Core concepts, definitions, and explanations for {topic} in {subject} {grade}"`

---

### 3. Frontend UI (Admin Panel)
#### [MODIFY] [ToolContentManager.jsx](file:///c:/empathai_updated_new/EmpathAI/EmpathaiFrontend/src/components/admin/aiknowledgebase/ToolContentManager.jsx)
- Register `ANALOGY` in `AI_TASKS`:
  ```javascript
  { key: 'ANALOGY', label: 'Analogy Storyteller', icon: '📖', desc: 'Creative analogies & stories' }
  ```
- **AnalogyEditor Component**: Create a component allowing admins to edit:
  - Concept name
  - Analogy Title, Base comparison, and Description mapping
  - Story Title, Narrative, and moral takeaway
  - Wire up the `<InlineImageUploader>` so admins can upload concept-specific illustrations.
- **Preview Drawer**: Include a structured preview displaying:
  - Toggle tabs: "💡 The Analogy" and "📖 The Story"
  - Beautiful visual presentation so admins can review how it looks.

## Verification Plan
1. Admin selects a topic and clicks "Generate Analogy Storyteller".
2. Verify output compiles into valid JSON structure and resolves successfully.
3. Open the **Preview Drawer** to verify toggle layout.
4. Click **Edit**, upload an image to one of the concepts, click **Save Changes**, and verify it persists.
